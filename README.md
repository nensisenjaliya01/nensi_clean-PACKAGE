# nensi-clean

A lightweight Python package for cleaning and normalizing text,
especially useful for PDFs and NLP preprocessing.

## Features
- Remove extra spaces
- Clean special characters
- Fix broken PDF line text

## Installation
```bash
pip install nensi-clean
